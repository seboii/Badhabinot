# Backend Services

Spring Boot services live here.

- `api-gateway`: public routing and JWT validation
- `auth-service`: authentication and token lifecycle
- `user-service`: profile/settings/consent state
- `monitoring-service`: monitoring domain logic and Spring-to-Python orchestration
