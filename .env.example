# Database
POSTGRES_USER=badhabinot
POSTGRES_PASSWORD=
POSTGRES_PORT=5432
REDIS_PORT=6379

# Internal auth
SECURITY_JWT_SECRET=
INTERNAL_API_KEY=

# Published ports
BACKEND_PORT=8080
VISION_SERVICE_PORT=8091
AI_SERVICE_PORT=8092
FRONTEND_PORT=3000

# Internal service URLs
AUTH_SERVICE_URL=http://backend:8080
USER_SERVICE_URL=http://backend:8080
MONITORING_SERVICE_URL=http://backend:8080
VISION_SERVICE_URL=http://vision-service:8091
AI_SERVICE_URL=http://ai-service:8092

# AI adapter
# AI_PROVIDER options:
#   "openai-compatible" — use any OpenAI-compatible API (requires AI_API_KEY)
#   "ollama"            — use a local Ollama instance (no API key needed)
#   "mock"              — deterministic offline mode for testing
# If AI_PROVIDER=openai-compatible and AI_API_KEY is empty, the service
# automatically falls back to mock mode so it stays functional.
AI_PROVIDER=openai-compatible
AI_API_BASE_URL=https://api.openai.com/v1
AI_API_KEY=
AI_MODEL_NAME=gpt-4.1-mini
AI_TIMEOUT_SECONDS=30
AI_READINESS_TIMEOUT_SECONDS=10
AI_MAX_RETRIES=2
AI_TEMPERATURE=0.1

# Ollama (used when AI_PROVIDER=ollama or for per-user LOCAL mode)
OLLAMA_BASE_URL=http://host.docker.internal:11434
OLLAMA_MODEL_NAME=llama3.2:3b

# Shared runtime settings
VISION_SERVICE_READ_TIMEOUT=PT10S
AI_SERVICE_READ_TIMEOUT=PT25S
LOG_LEVEL=INFO
USER_CONTEXT_CACHE_TTL=PT5M
ANALYSIS_JOB_CACHE_TTL=PT15M
ANALYSIS_JOB_KEY_PREFIX=badhabinot:monitoring:analysis-job:

# Frontend local development
VITE_API_BASE_URL=
VITE_DEV_PROXY_TARGET=http://localhost:8080

# Vision service — face data & session log storage paths (inside container)
FACE_DATA_DIR=/app/data/users
SESSION_LOG_DIR=/app/logs/sessions

# Optional JVM flags
BACKEND_JAVA_OPTS=
