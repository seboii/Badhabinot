# Shared Config

This package stores shared environment fragments and configuration guidance that apply across multiple services.

- `env/backend.env.example`: Spring service defaults and Redis-related settings
- `env/python-services.env.example`: FastAPI service settings
- `env/web.env.example`: frontend-specific environment values
