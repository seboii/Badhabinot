services:
  postgres:
    ports:
      - "${POSTGRES_PORT:-5432}:5432"

  redis:
    ports:
      - "${REDIS_PORT:-6379}:6379"

  backend:
    ports:
      - "${BACKEND_PORT:-8080}:8080"

  frontend-app:
    ports:
      - "${FRONTEND_PORT:-3000}:80"
