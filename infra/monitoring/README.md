# Monitoring Infra

This directory is reserved for observability assets such as dashboards, alerting rules, and metrics collectors.

The current local stack does not ship Grafana or Prometheus by default, but the directory exists so operational assets stay under `infra/` rather than leaking into application folders.
