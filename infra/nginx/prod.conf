# infra/nginx/prod.conf
# ─────────────────────────────────────────────────────────
# SUNUCU KONFİGÜRASYONU — HTTPS + SSL
#
# ŞU AN KULLANILMIYOR — docker-compose.prod.yml ile aktif edilir.
#
# Aktif etmek için:
#   1. api.badhabinot.com DNS → sunucu IP'si olduğundan emin ol
#   2. SSL sertifikası al (sunucuda):
#        sudo apt install certbot
#        sudo certbot certonly --standalone \
#          -d api.badhabinot.com -d badhabinot.com \
#          --email admin@badhabinot.com --agree-tos
#   3. docker-compose.prod.yml'deki yorumları kaldır
#   4. make prod-up
# ─────────────────────────────────────────────────────────

# upstream backend {
#     server backend:8080;
#     keepalive 32;
# }
#
# upstream frontend {
#     server frontend-app:80;
#     keepalive 16;
# }
#
# # HTTP → HTTPS yönlendirme
# server {
#     listen 80;
#     server_name api.badhabinot.com badhabinot.com www.badhabinot.com;
#
#     # Let's Encrypt doğrulama
#     location /.well-known/acme-challenge/ {
#         root /var/www/certbot;
#     }
#
#     location / {
#         return 301 https://$host$request_uri;
#     }
# }
#
# # Ana HTTPS sunucu
# server {
#     listen 443 ssl http2;
#     server_name api.badhabinot.com;
#
#     ssl_certificate     /etc/letsencrypt/live/api.badhabinot.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/api.badhabinot.com/privkey.pem;
#
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
#     ssl_prefer_server_ciphers off;
#     ssl_session_cache shared:SSL:10m;
#     ssl_session_timeout 1d;
#
#     add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
#
#     # ── SSE endpoint ─────────────────────────────────────
#     location = /api/v1/monitoring/chat/stream {
#         proxy_pass http://backend;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto https;
#         proxy_set_header Connection '';
#         proxy_buffering off;
#         proxy_cache off;
#         proxy_read_timeout 300s;
#         chunked_transfer_encoding on;
#         add_header X-Accel-Buffering no;
#     }
#
#     # ── API endpoint'leri → Backend ─────────────────────
#     location /api/ {
#         proxy_pass http://backend;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto https;
#         proxy_set_header Connection "";
#         proxy_connect_timeout 10s;
#         proxy_read_timeout 300s;
#         proxy_buffering off;
#         proxy_cache off;
#         add_header X-Accel-Buffering no;
#     }
#
#     # ── Actuator ─────────────────────────────────────────
#     location /actuator/ {
#         proxy_pass http://backend;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_connect_timeout 5s;
#         proxy_read_timeout 10s;
#         # Dışarıdan erişimi sınırla:
#         # allow 1.2.3.4;  # monitoring IP
#         # deny all;
#     }
#
#     # ── Frontend ─────────────────────────────────────────
#     location / {
#         proxy_pass http://frontend;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_connect_timeout 5s;
#         proxy_read_timeout 60s;
#     }
#
#     error_page 502 503 504 /50x.html;
#     location = /50x.html {
#         return 503 '{"error": "Servis başlatılıyor, lütfen bekleyin..."}';
#         add_header Content-Type application/json;
#     }
# }
